# Security and Privacy

## 数据处理

JobCollector 只在用户动作后读取当前页面展示的职位字段，保存在 `chrome.storage.local`。扩展没有后端、遥测、第三方分析、远程代码或云同步。CSV 仅由用户主动导出。

## 不采集的数据

- 账号凭据、Cookie、token；
- 招聘者姓名、联系方式、聊天记录；
- 浏览历史或当前职位之外的页面；
- 自动打开或通过接口额外获取的内容。

## 报告问题

远程仓库建立后，请通过仓库的私密安全报告渠道报告漏洞，不要在公开 issue 中粘贴真实职位、账号或浏览器数据。当前本地阶段可向维护者提供最小复现和完全脱敏的 fixture。

## 发布检查

发布前必须重新检查目标站点规则、浏览器扩展政策、manifest 权限、zip 内容、依赖与 fixture 敏感信息。受限实现不代表获得目标平台授权。
